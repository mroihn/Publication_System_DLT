{
  "name": "publication-system-smartcontract",
  "version": "1.0.0",
  "description": "Decentralized Publication System Smart Contracts for Polygon Amoy",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:gas": "REPORT_GAS=true hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "upgrade:amoy": "hardhat run scripts/upgrade.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@openzeppelin/hardhat-upgrades": "^3.6.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "@openzeppelin/contracts-upgradeable": "^5.1.0",
    "dotenv": "^16.4.0"
  }
}
